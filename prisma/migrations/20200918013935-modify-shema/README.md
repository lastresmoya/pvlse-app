# Migration `20200918013935-modify-shema`

This migration has been generated by rob Moya at 9/17/2020, 7:39:35 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Activities" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "name" TEXT NOT NULL,
    "price" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "place" TEXT NOT NULL,
    "startDate" DATETIME NOT NULL,
    "level" TEXT NOT NULL,
    "userJoin" INTEGER NOT NULL DEFAULT 0,
    "languages" TEXT NOT NULL,
    "userId" INTEGER,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Activities" ("id", "createdAt", "updatedAt", "name", "price", "description", "place", "startDate", "level", "languages", "userId", "userJoin") SELECT "id", "createdAt", "updatedAt", "name", "price", "description", "place", "startDate", "level", "languages", "userId", coalesce("userJoin", 0) AS "userJoin" FROM "Activities";
DROP TABLE "Activities";
ALTER TABLE "new_Activities" RENAME TO "Activities";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200918005524-add-new-schema..20200918013935-modify-shema
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -16,11 +16,11 @@
   description   String
   place         String
   startDate     DateTime
   level         String
-  userJoin      Int?
+  userJoin      Int      @default(0)
   languages     String
-  user          User?   @relation(fields: [userId], references: [id])
+  user          User?    @relation(fields: [userId], references: [id])
   userId        Int?
 }
 model User {
```


